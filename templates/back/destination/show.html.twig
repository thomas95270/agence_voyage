{% extends 'base.html.twig' %}

{% block title %}Destination{% endblock %}

{% block body %}
{% include "back/menu_back.html.twig" %}

    <h1 class="text-center text-info m-5">Destination</h1>

    <table class="table table_index text-center mx-auto">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ destination.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ destination.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ destination.description }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-warning rounded" href="{{ path('app_destination_edit', {'id': destination.id}) }}">Modifier</a>

    {{ include('back/destination/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}
