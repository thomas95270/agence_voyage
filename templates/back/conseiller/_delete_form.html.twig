<form method="post" action="{{ path('app_conseiller_delete', {'id': conseiller.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">

    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conseiller.id) }}">
    <div class=" d-flex justify-content-center">
        <button class="btn btn-danger rounded-pill col-1 m-3">
            Supprimer
        </button>
    </div>
</form>
